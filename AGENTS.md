# Anchored Summary — HelpdeskUltra (AI Panel: zero-result fix + UI redesign + build stabilization)

## Objective
- Fix `run_full_diagnostics` returning zero useful results to the LLM (health_score=0, empty critical_issues/report_text), and redesign the AI panel's ugly blue suggestion cards into a single compact "⚡ Действия ▼" dropdown with grouped actions. Both DONE and published. Plus stabilize `build.ps1` so FX-8320 doesn't saturate the chip and red-dot the OpenCode local server during builds — DONE (variant C). Plus **round 3 performance optimization** for slow old flash drive: session-list freeze removed, AI streaming no longer janks, SQLite/log/audit I/O moved off the UI thread. DONE and published.

## Important Details
- Russian UI/plan. AGENTS.md §15: no empty `catch { }`.
- GPU GTX 650 Ti (Kepler, no CUDA) — OpenCode uses **cloud models only** (user confirmed). CPU AMD FX-8320 — 4 modules / 8 threads sharing uncore + memory controller; full-chip load crashes system / reds OpenCode `localserver`.
- RAM 16 GB (10.4 free) not bottleneck.
- AI arch: `CloudLlmService`, `AiChatViewModel` (single-owner tool exec), `AiToolExecutor`, `AiSystemPromptBuilder`, `AiSuggestionEngine`, `ChatCommandRouter`, `AgentAuditLogger`, `UiAgentApprovalService`. Tab order: 0 Summary,1 Network,2 Hardware,3 Helpdesk,4 Report,5 KB,6 Settings,7 Plugins. AI = right-side panel.
- `FreeModels` (17) hardcoded in `AiChatViewModel`; live switch via `Settings.CloudModel`+`CloudLlmService.ApplySettings()`.
- VM prop misspelled `FullDiagnoticsCommand`. `AiPanelBridge` static (`OnRoute`/`Route`, `OnToast`/`ShowToast`). `RouteQueryAsync` routes to real `SendAsync`.
- Theme: `App.xaml` inlines IntelliNetDark; duplicate `Themes/IntelliNetDark.xaml` kept in sync. P0+P1 dark-theme fixes (ToolTip/ToggleButton/ScrollBar/Slider/DatePicker/Calendar) COMPLETE.
- **ROOT CAUSE (AI zero results)**: `CompositeTools.RunFullDiagnosticsTool` built summary via dead `ParseResult(result.Summary)` (scanned the one-line "OK=8 FAIL=2 WARN=10" for ✅/❌ markers — none found). `IntelliNetCommandRunnerAdapter.RunAsync` returned only `result.Message` (one-liner) as Summary. Both FIXED.
- **WPF .NET 9 quirk**: `System.ComponentModel.PropertyGroupDescription` does NOT exist in .NET 9 WPF ref assemblies — suggestion grouping done manually in the VM (no `CollectionViewSource`/`GroupStyle`).
- Tests 282/282 pass. Published EXE: `C:\Users\Mark1\Desktop\HelpdeskUltra.exe` + `F:\HelpdeskUltra.exe` (90,216,432 bytes @ 17:24, self-contained, compressed; SHA256 `8DEF9AAD…A8F6`). Note: round 3 grew the EXE slightly (Serilog.Sinks.Async added); P1-6…P1-11 added the audit window/settings.
- **PASSWORD/Vault removed (portable keys)**: API keys now live in `settings.json` → `CloudApiKeys` dict (portable, travels with the flash drive), NOT in machine-bound DPAPI `vault.dat`. Startup no longer shows `VaultUnlockWindow`; the window files were deleted. One-time migration in `SettingsService.LoadAsync` copies old keys from vault (if unlocked) into settings.json. `CloudLlmService`/`NotificationHubService` read keys from settings (SMTP password = `AppSettings.SmtpPassword`).
- **Build stabilization (variant C)**: `build.ps1` rewrites aggressive throttle — `DOTNET_BUILD_CONCURRENCY=1`, `DOTNET_gcServer=0`, `DOTNET_GCHeapCount=1`; build on **1 core (0x01)** at **`Idle`**. `Protect-Servers` isolates OpenCode/Hermes to cores 4-7 at **NORMAL** priority (no `High` boost — that boosted local-server to red dot). Watchdog re-pins `csc`/`msbuild`/`VBCSCompiler` to core 0 for 60s; main loop **suspends the whole build tree (`NtSuspendProcess`) for 0.5s every 1.5s** so the OpenCode `localserver` heartbeat survives. Uses `UseSharedCompilation=false`, `/nodeReuse:false`, kills VBCSCompiler/MSBuild post-deploy. Trade-off: build slower (~3-4 min dev / ~10 min self-contained publish).
- **Deploy timestamp note**: `dotnet publish` is incremental — with unchanged source (last code edit 17:06) it reuses the previously compiled EXE (TEMP `hu-publish`, timestamp 17:23:20) and `Copy-Item` preserves the source's LastWriteTime, so `F:\HelpdeskUltra.exe` keeps `17:23:22`. This is the genuine compile time and is correct; variant C only affects build throttle, not the binary. Verified: SHA256 of `F:\HelpdeskUltra.exe` == freshly published TEMP output (`B2398EB6…09784`, MATCH=True).
- **Round 3 (flash-drive perf) — user decisions**: **data stays on flash (Вариант Б)** but ALL I/O is async/off UI thread; **streaming shows plain monospace text (Вариант А)**, full markdown rendered ONCE on completion.
- **Round 3 fixes (all DONE)**:
  - *Session list freeze fixed*: `ChatSessionService` gained `ListSummariesAsync` (SELECT id/title/preview/updated_at only — no `messages_json` deserialize). `ChatSessionItem` refactored to hold `Id/Title/Preview/UpdatedAt` (+ lazy `Session`); `preview` column added to DB (pragma migration). `AiChatSessionManager.LoadSessionsAsync` runs inside `Task.Run`. `AiChatView.xaml.cs` `SessionsPopup_Open` no longer calls blocking `RefreshCommand.Execute(null)` — fires non-blocking `vm.RefreshSessionsAsync()` instead; selection `GetSessionAsync` wrapped in `Task.Run`.
  - *AI streaming jank fixed*: `ChatBubble` gained `StreamingText` + `IsStreaming`; during stream the XAML shows a plain `TextBox` (monospace, read-only) bound to `StreamingText`, and a `FlowDocumentScrollViewer` (markdown) only when `IsStreaming==false`. Streaming loop now accumulates into a `StringBuilder` and pushes plain text via `Dispatcher.BeginInvoke` (no per-chunk FlowDocument rebuild); on `chunk.Done` it calls `EndStream(finalText)` once (markdown built a single time). `Dispatcher.Invoke` per chunk replaced by `BeginInvoke`. Chat `ItemsControl` now virtualizes (`VirtualizingStackPanel` + `IsVirtualizing`/`CanContentScroll`).
  - *Flash I/O off UI thread*: DB services `ChatSessionService`/`KnowledgeBaseService`/`TaskBoardService` no longer open SQLite in constructor — open+schema moved to `InitializeAsync()` (runs in `Task.Run`); `App.xaml.cs` awaits `Task.WhenAll(...)` of the 3 services BEFORE window show and BEFORE KB seeding. `LoggerService` uses `WriteTo.Async` (Serilog.Sinks.Async added). `AgentAuditLogger` rewritten to enqueue lines into a `Channel<string>` consumed by a background `Task` (`File.AppendAllTextAsync`) — no sync disk write per tool call. `StorageViewModel` ctor no longer blocks on `_plugin.InitializeAsync().Wait()` — init runs in `Task.Run` and `RunDiagnosticsAsync` awaits it. `MetricTimelineControl` `%TEMP%` bug → real `Path.GetTempPath()`.

## Work State
### Completed
- **Программа улучшений (P0×5 → P1×6 → P2×10)** — утверждена («Весь список до P2»; офлайн-LLM исключён; radar-chart для axes). Трекер: tests 131 → **282**.
  - **P0-1 CommandRiskAnalyzer** (DONE, EXE 90 172 304 байт @ Desktop 22:59): `Core/Security/CommandRiskAnalyzer.cs` — `CommandRiskLevel {Safe,Normal,High}`, `MatchHighRisk(command)` возвращает метки-совпадения; паттерны: Remove-Item (любой), Remove-ADUser/Group/Computer, Format-Volume, Clear-Disk, Initialize-Disk, net user|group|localgroup /delete, rmdir /s, del /s, diskpart, `format X:`, bcdedit /remove|/delete, cipher /w, Set-ExecutionPolicy, Disable-NetAdapter, Stop/Restart-Computer, shutdown /s|/r, Stop/Restart-Service -Force, reg delete, Set-Service Disabled; `-WhatIf` = не High. Safe = Get-*/Test-*/ping/ipconfig/nslookup/... Normal = остальное. Интеграция: `RunPowerShellTool`/`RunCmdTool` — High **всегда** вызывает approval (даже при `AgentRequireApproval=false`), сообщение с перечнем рисков; `RunProcessTool` — High в whitelist-эксфолт → блок; `AiToolExecutor` — warning-лог High для run_powershell/run_cmd/run_process. `AgentBlockedArgs` substring-блоклист остаётся первым барьером. Тесты `CommandRiskAnalyzerTests`: 47 кейсов (inline+theory). 178/178 pass.
  - **P0-2 PromptSanitizer** (DONE): `Core/Security/PromptSanitizer.cs` — маркеры `<<<untrusted-data-start/end>>>`, `ShouldWrap(toolName)` (24 недоверенных тула), `WrapUntrusted(content, source)` (идемпотентен), `BuildPromptRules()`. Обёртка на едином чокпойнте `CloudLlmService.cs` (хелпер `ToolContentForLlm` для `["content"]` и `ToolResult`); `AiSystemPromptBuilder` (UI) дополнен правилами. Тесты `PromptSanitizerTests`: 21 шт. 199/199 pass.
  - **P0-3 HealthBreakdown + radar** (DONE): `UltimateDiagnosticReport` + record `HealthAxis(Name, RawCategory, Score, Ok, Warn, Fail)` и `Axes`; `Core/Services/HealthBreakdownBuilder.cs` группы по категории + RU-имена (Сеть/DNS/Интернет/Службы/Диски/RAM/Безопасность/SMB/VPN/Kerberos/AD/LDAP/Прочее); `UnifiedDiagnosticEngine.DiagnosticsCompleted` + `Axes` в отчёте (включая `BackgroundDiagnosticService`). UI: `Views/IntelliNet/HealthRadarChartControl.xaml(.cs)` (Canvas-радар без либ), `SummaryTab.xaml` Row 4 = timeline + радар справа, `SummaryViewModel.HealthAxes`+`UpdateDiagnosticAxes`, подписка в `App.xaml.cs`. Тесты `HealthBreakdownBuilderTests`: 9 шт. 207/207 pass.
  - **P0-4 DiagnosticSnapshotStore + DiagnosticDiffService** (DONE, EXE 90 195 527 @ Desktop 13:57): `Core/Services/DiagnosticSnapshotStore.cs` — снапшоты `data/diagnostics/{host}/{yyyyMMdd-HHmmssfff}.json` (SelfCollision-защита `_N`), host = `Environment.MachineName`, SaveAsync/ListAsync/LoadAsync/LatestBeforeAsync. `Core/Services/DiagnosticDiffService.cs` — record `DiagnosticDiff(Host, Previous/CurrentTimestamp, Previous/CurrentHealth, HealthDelta, NewIssues, ResolvedIssues, AxisDeltas)`; сравнение по ключу `Title|Category` (lower), оси по дельте Score (+новая/исчезла). `UltimateDiagnosticReport` + свойство `Host`. Регистрация в DI (`ServiceCollectionExtensions`), интеграция в `RunFullDiagnosticsTool.ExecuteAsync` — после диагностики сохраняет снапшот + `result["snapshot"]`, при наличии предыдущего — `result["delta"]` (health_delta/new_issues/resolved_issues/axis_deltas) для LLM (тренды). UI: кнопка «Сравнить с предыдущей» на «Сводке» (`SummaryViewModel.CompareWithPreviousCommand`) → `Views/DiagnosticsDiffWindow.xaml` + `DiagnosticsDiffModel` (готовые строки, без капризного StringFormat в timestamp — MC1000), TabControl «Новые проблемы»/«Решённые». Тесты `DiagnosticSnapshotStoreTests`(5)+`DiagnosticDiffServiceTests`(7): 12 шт. 216/216 pass.
  - **P0-5 guardrails-тесты + вынос ToolLoopDetector** (DONE, EXE 90 195 677 @ Desktop 14:14): `Core/Security/ToolLoopDetector.cs` — класс анти-лупа (повтор одинакового `name|argsRaw` ≥3 раз → true), заменяет inline `lastCallKey/repeatCount` в обоих ветках `ChatStreamAsync` (stream+non-stream). `CloudLlmService` получил опциональный `HttpClient? http = null` в ctor (для тестов). Тесты: `ToolLoopDetectorTests` (8: повторы/чередование/сброс/кастом-лимит/чувствительность к регистру/порядку аргументов), `CloudLlmServiceSecurityTests` (5: маркеры на недоверенном run_powershell, отсутствие обёртки на доверенном get_uptime, deny-approval → tool не исполняется, approve → исполняется + маркер, инжекция с уже-маркерами не двойная — `JsonSerializer` экранирует `<`/`>` в `\u003C/\u003E`, поэтому wrap ровно один раз; FakeHttpHandler с SSE-пайплайном), `AiToolExecutorTests` +3 (High-риск всегда approval даже при AgentRequireApproval=false + warning-лог, блок-аргумент rejected без исполнения, `-WhatIf` не High не требует approval). ВАЖНО для тестов: `CapturingLogger.LogWarning` НЕ делает `string.Format` на Serilog-шаблонах с `{Name}` (FormatException) — просто конкатенация msg+args. 232/232 pass.
  - **P1-6 фоновый мониторинг CPU/RAM/диск** (DONE): `Core/Services/BackgroundDiagnosticService.cs` (public `BuildReport`, `BackgroundCheckEvaluator`), настройки в config/`AppSettings`/UI, подписка в `SummaryViewModel`, `ScheduledDiagnostics` стартует как ежедневная регрессия. Тесты `BackgroundDiagnosticServiceTests`: 25 шт.
  - **P1-7 единый пайплайн уведомлений → ToastWindow** (DONE): `UI/Helpers/NotificationUIBridge.cs` (Hub `OnNotification` → `ToastWindow`, гейт `EnableNotifications`, `Reset()` для тестов) + Wire в `App.xaml.cs`. Тесты `NotificationUIBridgeTests`: 12 шт.
  - **P1-8 retention снапшотов + дельта в PDF** (DONE): `DiagnosticSnapshotStore.RetentionCount=50`, `PruneAsync`, `LatestBeforeAsync`; `AppConfig.SnapshotRetention` (`app-config.json "snapshot_retention": 50`); дельта в PDF через `ReportViewModel.GenerateWithSnapshotDeltaAsync`/`BuildPdf`. Тесты: 12 шт.
  - **P1-9 KbSeeder: порт 22 HD-статей** (DONE): подтверждено — `KbSeeder.GetHduArticles()` уже содержит полный порт статей из `scripts/seed_kb.py` (HDU-ARCH, HDU-AI-PIPE, HDU-AI-TOOLS, HDU-DIAG-PROC, HDU-AI-PROV, HDU-INTELLI, HDU-DEPLOY, HDU-KB-GUIDE, HDU-SEC-APPROVAL = все 9 из .py) плюс доп. HDU-*/KB-* (HDU-FAIL-*, HDU-REPORT, HDU-AI-KBCONTEXT, HDU-DB-WAL, KB-DNS/PRINT/DOMAIN/SMB/RDP/WU/DISK/AV/SLOW). `KbSeederTests` ожидает >50 `UpsertArticleAsync` на пустой KB + `BackfillEmptyDiagnosisResolutionAsync` всегда. 272/272 pass.
  - **P1-10 настройки агента (UI + runtime-гейт)** (DONE): `SettingsViewModel.cs` — `AgentModeEnabled` (default true), `AgentMaxToolRounds` (Clamp 1–1000, default 100), `AgentAllowedPaths` (default `data,logs,kb,templates`), `AgentBlockedArgs` (default `format,rmdir,rd,del,remove,reg delete,reg remove,net user,sfc,dism,bcdedit,bootrec,cipher,diskpart`); load/save/reset + OnPropertyChanged. `SettingsView.xaml` GroupBox «🤖 AI Agent»: CheckBox «Включить агента», Slider 1–200+TextBox «Макс. раундов», TextBox «Разрешённые пути», TextBox «Блоклист аргументов». `AiToolExecutor.GetToolsSchema()` возвращает `[]` при `!AgentModeEnabled` (тулы не передаются LLM). `AiSystemPromptBuilder.BuildAsync` добавляет правило «режим агента ОТКЛЮЧЁН» при выключенном агенте. Тесты `AiToolExecutorTests`: `GetToolsSchema_AgentModeDisabled_ReturnsEmpty`, `...Enabled_ReturnsTools`. 274/274 pass.
  - **P1-11 аудит агента: QueryAsync + окно + экспорт** (DONE): `Core/Services/AgentAuditLogger.cs` — record `AgentAuditEntry(TimestampUtc, Tool, ToolName, Success, DurationMs, ResultSummary, ArgsJson)`, `LogPath`, `QueryAsync(tool, success, fromUtc, toUtc, limit=500, ct)` (читает JSONL, фильтрует по подстроке тула/статусу/диапазону UTC, newest-first, limit; `ts` разбирается через `DateTimeOffset.TryParse` Invariant+AssumeUniversal); приватный `ParseLine` (поддерживает строковый ISO и числовой unix-ms, не парсит — DateTime.UtcNow). `UI/ViewModels/AgentAuditLogViewModel.cs` — `AuditEntryRow`, `Entries`, `ToolFilter`/`FailedOnly` (живой refilter), `StatsText`, `ExportTxtAsync` (TSV) / `ExportCsvAsync` (`;`), экранирование разделителей. `UI/Views/AgentAuditWindow.xaml(.cs)` — виртуализованный ListBox (время/тул/args/результат/статус), «Обновить», «Экспорт TXT/CSV». Кнопка «📜» в шапке `AiChatView.xaml` → `AiChatViewModel.OpenAuditCommand` → `OpenAuditWindow()`. Тесты `AgentAuditLoggerTests`: 8 шт. (запись/фильтры/порядок/limit/диапазон/truncate/отсутствие файла; `WaitFlushAsync(logger, expectedLines)` опрашивает число строк, а не только создание файла). **282/282 pass.**
- **Dark-theme P0+P1 fixes** (published earlier) — ToolTip/ToggleButton/ScrollBar/Slider/DatePicker/Calendar dark; duplicate ScrollBar key removed.
- **AI logic fix**:
  - `ShellTools.cs` `IntelliNetCommandRunnerAdapter.RunAsync` now enriches `Summary` with: total OK/FAIL/WARN, `result.Message`, top-25 `GetAttention()` entries (marker by Level: ❌/⚠️/ℹ️), and `GetLogTail(2000)`.
  - `CompositeTools.cs` `RunFullDiagnosticsTool.ExecuteAsync` rewritten: counts from `result.Pass/Fail/Warn`; `health_score = pass/total*100`; `critical_issues`/`warnings` parsed from enriched Summary (❌/FAIL/ERROR, ⚠️/WARNING/рекоменд); `report_text` always built (from `DiagnosisAnalyzer` if present, else fallback with health + summary); **removed dead `ParseResult`/`CountOccurrences`**.
- **UI redesign**:
  - `AiChatView.xaml` Row 2: removed blue `#16243A` Suggestion cards → compact `Border` bar with `QuickActions` chips + single `ToggleButton "⚡ Действия ▼"` (TwoWay-bound to `Popup.IsOpen`).
  - `Popup` shows suggestions **grouped by Category** via manual grouping (nested `ItemsControl`), category header + hover-highlight items. Contextual suggestions now hidden in the dropdown (no more cards blocking chat).
  - `AiChatViewModel.cs`: added `SuggestionGroup` class + `SuggestionGroups` ObservableCollection, populated in `SyncSuggestions`→`RebuildSuggestionGroups` (groups by `AiSuggestion.Category`).
  - Removed `CollectionViewSource` resource and code-behind `PropertyGroupDescription` (type absent in .NET 9).
- **Build stabilization variant C**: rewrote `build.ps1` (1-core `Idle` build, no `High` boost for OpenCode, GC limits, child re-pin watchdog, `NtSuspendProcess`/`NtResumeProcess` throttle 1.5s/0.5s).
- **Builds**: `dev` and `deploy` both **0 errors**; "Deployed to F:\" present. EXE `F:\HelpdeskUltra.exe` updated (timestamp 17:23:22 = genuine compile time; SHA256 verified against fresh publish). AGENTS.md updated with variant C notes.
- User confirmed: red dot `localserver` appears ONLY during build; models are cloud-only.
- **Chat UI controls + resize + scrollbar polish**:
  - `AiChatView.xaml` header: added «➕ Новая сессия» (`NewSessionCommand`) and «🗑 Очистить историю» (`ClearHistory_Click`, с подтверждением) buttons.
  - `AiChatView.xaml.cs`: `ClearHistory_Click` shows `MessageBox` confirm → calls `ClearHistoryCommand` (деструктивно, с подтверждением).
  - `AiChatViewModel.cs`: `NewSessionAsync` now cancels active generation (`_cts?.Cancel()`) and resets `IsGenerating/HasError/IsAgentThinking/AgentStatusText/AgentCurrentTool/AgentRound/AgentToolCount` for a clean session.
  - `MainWindow.xaml`: `SizeToContent="WidthAndHeight"` → `Height` (enables horizontal resize via existing `GridSplitter`); `AiPanelCol` MinWidth 260 / MaxWidth 900; `ResizeDirection="Columns"` on both splitters.
  - `App.xaml`: ScrollBar style got key `IntelliNetScrollBar` + implicit `BasedOn` proxy (pill everywhere).
  - `MainWindow.xaml`: «ВНИМАНИЕ» log got scoped ScrollBar style (pill like main log). Removed dead rectangular ScrollBar style from `Themes/IntelliNetDark.xaml`.
- **Round 3 (flash-drive perf) — DONE**:
  - *Session list*: `ChatSessionService.ListSummariesAsync` (id/title/preview/updated_at only) + `preview` DB column (pragma migration); `ChatSessionItem` now `Id/Title/Preview/UpdatedAt` (+ lazy `Session`); `AiChatSessionManager.LoadSessionsAsync` in `Task.Run`; `SessionsPopup_Open` fires non-blocking `RefreshSessionsAsync()` instead of blocking `RefreshCommand.Execute`. No more UI freeze.
  - *AI streaming*: `ChatBubble.StreamingText`+`IsStreaming`; XAML shows plain monospace `TextBox` during stream, `FlowDocumentScrollViewer` (markdown) only when `IsStreaming==false`; loop accumulates in `StringBuilder` + pushes via `Dispatcher.BeginInvoke`, `EndStream(finalText)` once on `chunk.Done`. Chat `ItemsControl` virtualizes.
  - *Flash I/O off UI*: `ChatSessionService`/`KnowledgeBaseService`/`TaskBoardService` open SQLite in `InitializeAsync()` (called via `Task.Run` in `App.xaml.cs` before show + before KB seeding); `LoggerService` uses `WriteTo.Async` (Serilog.Sinks.Async pkg added); `AgentAuditLogger` rewritten to `Channel<string>` + background `File.AppendAllTextAsync`; `StorageViewModel` ctor no longer `.Wait()`s plugin init (runs in `Task.Run`, awaited in `RunDiagnosticsAsync`); `MetricTimelineControl` `%TEMP%` → `Path.GetTempPath()`.

### Active / Pending verification
- Awaiting user confirmation that app is now smooth on the flash drive: session list opens without freeze, AI streaming no longer janks, startup not blocked by slow flash I/O.
- P1-11 DONE: журнал аудита агента (окно + фильтры + экспорт TXT/CSV) — awaiting user verification.

### Blocked
- (none)

## Next Move
- **P2-13…P2-22**: следующие пункты утверждённого плана (группа P2). Группа P1 (P1-6…P1-11) полностью закрыта.
- Публикация свежего EXE на Desktop после P1-11 (single-file, self-contained).
- User to launch `HelpdeskUltra.exe` and verify guardrails + audit journal.

## Relevant Files
- `src/HelpdeskUltra.Core/Tools/CompositeTools.cs` — `RunFullDiagnosticsTool.ExecuteAsync` rewritten; saves snapshot + delta into result for LLM.
- `src/HelpdeskUltra.Core/Services/DiagnosticSnapshotStore.cs` — снапшоты `data/diagnostics/{host}/{yyyyMMdd-HHmmssfff}.json`, SelfCollision `_N`.
- `src/HelpdeskUltra.Core/Services/DiagnosticDiffService.cs` — `DiagnosticDiff` record; сравнение по `Title|Category` (lower), дельты осей.
- `src/HelpdeskUltra.Core/Models/UltimateDiagnosticReport.cs` — + `Host`, + `Axes` (P0-3).
- `src/HelpdeskUltra.Core/Services/HealthBreakdownBuilder.cs` — группы по категории + RU-имена, score = Ok/(Ok+Fail)*100, Warn по Severity.
- `src/HelpdeskUltra.Core/Services/UnifiedDiagnosticEngine.cs` — `DiagnosticsCompleted` event, `AggregateResult(allSteps)`.
- `src/HelpdeskUltra.Core/Extensions/ServiceCollectionExtensions.cs` — DI: `DiagnosticSnapshotStore` + `DiagnosticDiffService`, переданы в `RunFullDiagnosticsTool`.
- `src/HelpdeskUltra.Core/Security/CommandRiskAnalyzer.cs`, `src/HelpdeskUltra.Core/Security/PromptSanitizer.cs`, `src/HelpdeskUltra.Core/Security/ToolLoopDetector.cs` — P0-1/P0-2/P0-5 (анти-луп вынесен из ChatStreamAsync).
- `src/HelpdeskUltra.Core/Tools/ShellProcessTools.cs`, `src/HelpdeskUltra.Core/Tools/ShellTools.cs` — High-риск approval/block (run_powershell/run_cmd/run_process).
- `src/HelpdeskUltra.Core/Services/CloudLlmService.cs` — `ToolContentForLlm` — единый чокпойнт обёртки untrusted-данных; + `HttpClient? http = null` в ctor; `ToolLoopDetector` в обеих ветках ChatStreamAsync.
- `src/HelpdeskUltra.UI/Views/DiagnosticsDiffWindow.xaml(.cs)`, `src/HelpdeskUltra.UI/Views/DiagnosticsDiffModel.cs` — окно сравнения (готовые строки).
- `src/HelpdeskUltra.UI/Views/IntelliNet/SummaryTab.xaml` — Row 4 radar + кнопка «Сравнить с предыдущей».
- `src/HelpdeskUltra.UI/ViewModels/IntelliNet/SummaryViewModel.cs` — `CompareWithPreviousCommand`, `HealthAxes`, `UpdateDiagnosticAxes`, внутренний `IDataPathProvider`.
- `src/HelpdeskUltra.UI/Views/IntelliNet/HealthRadarChartControl.xaml(.cs)` — Canvas-радар.
- `src/HelpdeskUltra.Tests/` — `CommandRiskAnalyzerTests.cs`, `PromptSanitizerTests.cs`, `HealthBreakdownBuilderTests.cs`, `DiagnosticDiffServiceTests.cs`(+snapshot-тесты), `ToolLoopDetectorTests.cs`, `CloudLlmServiceSecurityTests.cs`, `BackgroundDiagnosticServiceTests.cs`, `NotificationUIBridgeTests.cs`, `AgentAuditLoggerTests.cs` — итого 282.
- `src/HelpdeskUltra.Core/Services/AgentAuditLogger.cs` — `AgentAuditEntry` + `QueryAsync` + `ParseLine` (P1-11).
- `src/HelpdeskUltra.UI/ViewModels/AgentAuditLogViewModel.cs`, `src/HelpdeskUltra.UI/Views/AgentAuditWindow.xaml(.cs)` — окно журнала аудита агента + экспорт TXT/CSV (P1-11).
- `src/HelpdeskUltra.UI/Views/AiChatView.xaml` (кнопка «📜») + `src/HelpdeskUltra.UI/ViewModels/AiChatViewModel.cs` (`OpenAuditCommand`/`OpenAuditWindow`) — вход в журнал (P1-11).
- `src/HelpdeskUltra.UI/ViewModels/SettingsViewModel.cs`, `src/HelpdeskUltra.UI/Views/Settings/SettingsView.xaml`, `src/HelpdeskUltra.UI/Services/AiToolExecutor.cs`, `src/HelpdeskUltra.UI/Services/AiSystemPromptBuilder.cs` — настройки агента + runtime-гейт (P1-10).
- `C:\Users\Mark1\Desktop\HelpdeskUltra.exe` — published EXE (90,216,432 bytes @ 17:24:20, self-contained, compressed, SHA256 `8DEF9AAD…A8F6`). Mirror: `F:\HelpdeskUltra.exe`.
- **Publish note (variant C too slow)**: `build.ps1 deploy` (suspend-throttle loop) не уложился в 25 мин и оставил orphan-MSBuild — для публикации быстрее прямой `Start-Process dotnet publish ... -p:PublishSingleFile ... -o $env:TEMP\hu-publish2` с `PriorityClass='BelowNormal'` + `ProcessorAffinity=0x0F` (без suspend-цикла). Publish успешен, EXE 90,216,432 @ 17:24:20.
